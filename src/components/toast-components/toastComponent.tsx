import { AnimatePresence, motion } from "framer-motion";

const Toast = ({}) => {
  return (
    <AnimatePresence>
      <motion.div className=""></motion.div>
    </AnimatePresence>
  );
};

export default Toast;
